import angular from 'angular';
import accountComponent from './account.component';

export default angular.module('app.components.main.settings.account', [])
  .component('account', accountComponent);
